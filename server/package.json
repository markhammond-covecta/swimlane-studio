{
  "name": "swimlane-mcp",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "MCP server that renders swimlane diagrams (SVG, PNG, ASCII) from the Swimlane Studio DSL.",
  "bin": {
    "swimlane-mcp": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "smoke": "node smoke.js",
    "build:app": "node build-app.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^0.4.2",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@resvg/resvg-js": "^2.6.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
